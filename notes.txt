python training_script.py --data_dir ./data --batch_size 1 --epochs 10 --lr 0.001

--> anchor ora sto predendo quelli predefiniti di COCO, si potrebbe utilizzare KMeans per tirare fuori anchors ad hoc per BraTS2020

--> utilizzare la funziona autobalance è utile se vediamo problemi di convergenza
    brain gliomas hanno pattern diversi da COCO, ma i pesi fissi sono più stabili per iniziare.
    in caso si decidesse di utilizzarlo, bisognerebbe implementare la funzione

python inference.py \
  --data_dir /path/to/brats \
  --split val \
  --weights /path/to/your_checkpoint.pt \
  --img_size 640 \
  --batch_size 8 \
  --conf_thresh 0.25 \
  --iou_thresh 0.45 \
  --max_dets 50 \
  --save_dir ./runs/inference/exp1 \
  --save_vis --save_interval 100
